service: serverless-REST-boilerplate

provider:
  name: aws
  runtime: nodejs10.x
  stage: dev
  region: us-east-2

package:
  individually: true
  exclude:
    - README.md

functions:
  register:
    handler: src/routes/users/register.handler
